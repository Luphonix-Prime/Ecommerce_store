{
    "build": {
      "builder": "Nixpacks",
      "buildCommand": "pip install -r requirements.txt"
    },
    "deploy": {
      "startCommand": "gunicorn your_project.wsgi:application --bind 0.0.0.0:8000"
    }
  }
  